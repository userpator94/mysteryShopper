# Mystery Shopper - Frontend

Фронтенд приложения для тайных покупателей, интегрированный с реальным API сервером.

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 18+
- Запущенный API сервер на `localhost:3000`

### Установка и запуск
```bash
# Установка зависимостей
npm install

# Запуск фронтенда (требует запущенный API сервер на порту 3000)
npm run dev
```

### Сборка для продакшена
```bash
npm run build
```

## 📁 Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
├── pages/              # Страницы приложения
│   ├── OffersListPage.ts    # Страница списка предложений
│   ├── OfferDetailPage.ts   # Детальная страница предложения
│   ├── FavoritesPage.ts     # Страница избранного
│   ├── OrderHistoryPage.ts  # История заказов
│   └── ProfilePage.ts       # Профиль пользователя
├── services/           # Сервисы для работы с API
│   └── api.ts              # API клиент
├── types/              # TypeScript типы
├── router/             # Роутинг
└── utils/              # Утилиты
```

## 🔌 Интеграция с API

Фронтенд интегрирован с реальным API сервером, запущенным на `localhost:3000`:

### Предложения
- `GET /api/offers` - Получить список предложений
- `GET /api/offers/:id` - Получить предложение по ID
- `POST /api/offers/:id/favorite` - Переключить избранное

### Структура данных предложения
```typescript
interface Offer {
  id: string;
  image_id?: string;
  title: string;
  numeric_info: number;
  tags: string;
  is_promo: boolean;
  employer_id: string;
  start_date: string;
  end_date: string;
  is_active: boolean;
  price: string;
  max_participants: number;
  current_participants: number;
  description: string;
  requirements: string;
  location: string;
  created_at: string;
  updated_at: string;
  employer_name: string;
  employer_surname: string;
  employer_company: string;
  image_url?: string;
  image_alt_text?: string;
  available_slots: number;
}
```

## 🎯 Функциональность

### Страница предложений (`/offers`)
- ✅ Загрузка данных из реального API `/api/offers`
- ✅ Отображение состояний загрузки, ошибок и пустого списка
- ✅ Поиск по названию и описанию
- ✅ Фильтрация по категориям через URL параметры
- ✅ Добавление/удаление из избранного
- ✅ Переход к детальной странице предложения
- ✅ Адаптивный дизайн с изображениями
- ✅ Отображение промо-предложений
- ✅ Информация о количестве участников и датах

### Состояния интерфейса
- **Загрузка**: Спиннер с текстом "Загрузка предложений..."
- **Ошибка**: Сообщение об ошибке с кнопкой "Попробовать снова"
- **Пустой список**: Сообщение "Предложения не найдены"
- **Успех**: Список предложений с карточками

## 🛠 Технологии

- **Frontend**: TypeScript, Vite, CSS (Tailwind-подобные классы)
- **API**: RESTful API с JSON
- **Прокси**: Vite dev server проксирует `/api` на `localhost:3000`

## 📱 Адаптивность

Приложение оптимизировано для мобильных устройств с поддержкой:
- Touch-событий
- Адаптивной сетки
- Оптимизированных размеров элементов

## 🔧 Разработка

### Настройка API
API сервер должен быть запущен на `localhost:3000`. Прокси настроен в `vite.config.ts`:

```typescript
proxy: {
  '/api': {
    target: 'http://localhost:3000',
    changeOrigin: true,
    secure: false,
  }
}
```

### Отладка
- Логи прокси доступны в консоли Vite dev server
- API запросы можно отслеживать в Network tab браузера
- Ошибки API отображаются в консоли браузера

## 🚨 Важные замечания

1. **API сервер**: Убедитесь, что API сервер запущен на `localhost:3000` перед запуском фронтенда
2. **CORS**: API сервер должен поддерживать CORS для `localhost:5173`
3. **Изображения**: Пути к изображениям должны быть корректными относительно API сервера
