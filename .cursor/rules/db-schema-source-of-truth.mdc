---
description: Источник истины по схеме БД — дамп msDB
alwaysApply: true
---

# Схема БД: источник истины

При вопросах о **структуре базы данных** (таблицы, поля, типы, индексы, представления, триггеры) ориентироваться на файл:

`../mysteryShopperDB/dump-msDB-202603271607.sql`

(полный путь: `/Users/ilamartynenko/Documents/YH repos/mysteryShopperDB/dump-msDB-202603271607.sql` относительно соседнего репозитория `mysteryShopperDB`).

Файл **`mysteryShopperDB/db structure.sql`** не считать актуальным эталоном, если он расходится с дампом — при расхождениях **приоритет у дампа**.

При изменении схемы в проде/тесте: обновлять понимание по новому дампу или явно указывать пользователю обновить этот файл.
